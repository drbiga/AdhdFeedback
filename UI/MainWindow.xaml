<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="MainWindow"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        Width="160"
        Height="60"
        >
    <Window.Resources>
        <sys:Double x:Key="LightSize">40</sys:Double>
    </Window.Resources>
    <Grid>
        <StackPanel x:Name="EllipseStackPanel" Background="#111" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Ellipse x:Name="RedLight" Width="{StaticResource LightSize}" Height="{StaticResource LightSize}" Margin="4"
                 Fill="#f33"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center" />

            <Ellipse x:Name="YellowLight" Width="{StaticResource LightSize}" Height="{StaticResource LightSize}" Margin="4"
                 Fill="#ee3"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center" />

            <Ellipse x:Name="GreenLight" Width="{StaticResource LightSize}" Height="{StaticResource LightSize}" Margin="4"
                 Fill="#3c3"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center" />
        </StackPanel>
    </Grid>
</Window>
